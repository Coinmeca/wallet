{
    "installCommand": "yarn install && git submodule update --init --recursive && cd ui-kit && git fetch origin && git checkout main && git pull origin main && cd .. && yarn workspace @coinmeca/ui install && yarn workspace @coinmeca/ui build && yarn workspace @coinmeca/wallet-provider install && yarn workspace @coinmeca/wallet-provider build"
}
