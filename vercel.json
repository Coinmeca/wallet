{
    "installCommand": "echo $GITHUB_TOKEN && yarn install && export GITHUB_TOKEN=${GITHUB_TOKEN} && sed -i 's|https://github.com|https://${GITHUB_TOKEN}@github.com|' wallet-provider/.gitmodules && git submodule update --init --recursive && yarn build && yarn build:provider:sdk"
}
