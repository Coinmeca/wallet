{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "jsx": "preserve",  // use preserve for JSX in this folder
  },
  "exclude": ["../../node_modules", "../../dist", "../../rollup.config.*", "../../../src"]
}
