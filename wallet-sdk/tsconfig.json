{
    "extends": "./tsconfig.rollup.json",
    "compilerOptions": {
        "jsx": "preserve" // use preserve for JSX in this folder
    },
    "include": ["src/contexts", "src/index.ts"],
    "exclude": ["!src/contexts"]
}
